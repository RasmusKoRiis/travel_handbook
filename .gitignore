# ── macOS / Windows cruft ───────────────────────────────────────
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ── IDE / editor folders ────────────────────────────────────────
.vscode/
.idea/
*.code-workspace
*.sublime-*

# ── Node / tooling artefacts (safe even if you’re not using them) ─
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
build/
.tmp/
.cache/
.eslintcache
coverage/

# ── Environment & secrets (just in case) ───────────────────────
.env
.env.*            # .env.local, .env.production, etc.
*.local           # e.g. firebase.local

# ── OS-generated resource forks ─────────────────────────────────
*.orig
*.swp
*~
._*

# ── Images generated by design tools (keep sources, ignore previews) ─
*.sketch/
*.fig/

# ── Service-worker cache (if you decide to persist to disk) ─────
sw-cache/
